/**
 * @typedef {{id: string, name: string, permissions: string, icon: string}} guild
 */

/**
 * @typedef {{id: string, name: string, permissions: string}} channel
 */

/**
 * @typedef {{id: string, name: string, permissions: string}} role
 */

export const Types = {}